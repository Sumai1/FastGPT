# 企业产品智能客服 RAG 实施 TODO

状态：`[ ]` 待执行，`[-]` 执行中，`[x]` 已完成，`[!]` 等待外部输入。  
当前阶段：阶段一至阶段七、V1.4 控制台和 V1.5 双前端产品闭环代码已完成；正在进行最终质量门禁、构建、备份和灰度部署。试点产品资料、200 题验收和 Rerank 仍为后续外部输入项。

## 0. 规划门禁

- [x] 确认阶段一至阶段七为一期完整交付范围。
- [x] 确认产品物理隔离、受众和版本逻辑过滤。
- [x] 确认现有 OpenAPI Key 绑定最高受众且请求不得提权。
- [x] 确认编辑、审核分离和禁止自审。
- [x] 确认型号/版本缺失时的追问和通用知识规则。
- [x] 确认独立客服业务 API 和内置 `projects/app` 客服入口。
- [x] 确认服务端转人工规则优先。
- [x] 确认复用 AI Proxy 和默认品牌基线。
- [x] 完成 FastGPT 现有能力复用审计。
- [x] 将规划整理为“直接复用、配置组装、必要二开”三层结构。
- [x] 同步 V1.1 项目规划、需求设计、功能开发文档和 TODO。
- [x] 用户最终评审 V1.1 规划文档并明确进入开发。
- [!] 收集模型渠道、三个试点产品资料、问题集、人工客服和生产环境信息。

## 1. 阶段一：源码和环境验证

- [x] 确认正式 FastGPT 发布基线；记录 commit、Node、pnpm 和镜像版本。
- [!] 配置官方 upstream，不改变现有 origin（当前 `.git` 只读，执行命令已记录）。
- [!] 获得授权后建立开发分支和公司版本标识（当前 `.git` 只读，执行命令已记录）。
- [x] 安装依赖并验证现有开发环境的 MongoDB、PGVector、Redis、MinIO 和 AI Proxy。
- [!] 获取 DeepSeek、Embedding 和 Reranker 测试渠道与密钥。
- [!] 配置真实模型逻辑名称，创建正式测试团队、App、dataset 和 OpenAPI Key（等待渠道密钥及试点资料）。
- [!] 使用真实资料验证文件/QA 导入、分块、训练状态、错误重试和检索测试。
- [x] 从源码、接口和自动化测试验证混合检索、Rerank、引用、流式聊天、会话、反馈、日志和用量复用链路。
- [x] 验证现有 dataset write/manage 权限的数据范围，并确认 manage 包含 write 的现有行为。
- [x] 定位客服 collection 白名单的最小搜索注入点并完成技术验证记录。
- [x] 定位客服绑定 Key 在通用 completions 入口的最小拒绝检查点。
- [x] 输出阶段一复用清单、必要修改点和基线验证记录。

## 2. 阶段二：系统裁剪与品牌

- [x] 通过现有配置设置“企业产品智能客服”、Logo、favicon 和 `#3370FF` 主色基线。
- [x] 调整客服页标题、欢迎语和默认提示词，保留现有登录与系统配置能力。
- [x] 增加桌面端和移动端客服入口，不删除 FastGPT 底层能力。
- [x] 未为已有品牌能力重复增加配置字段。
- [x] 定向 ESLint、TypeScript 类型检查和 Next.js 生产构建通过。

## 3. 阶段三：产品和知识管理

### 3.1 产品与绑定

- [x] 创建 customerService 公共常量、Zod schema 和类型。
- [x] 实现产品大类、系列、型号和软硬件版本 schema/entity/service。
- [x] 使用 `defineIndex` 声明型号、系列和版本唯一索引。
- [x] 实现产品 CRUD、停用/停产、别名和版本有效期。
- [x] 实现型号与现有 dataset 的绑定和权限校验。
- [x] 实现产品管理 API/OpenAPI 和管理页面。

### 3.2 知识治理

- [x] 实现 collection 引用式治理记录和审计记录，不复制正文/chunk/向量。
- [x] 治理页面引用现有 dataset/collection，待发布知识保持 `forbid=true`，不复制正文和向量。
- [x] 发布校验直接读取现有 collection 训练状态，不创建客服训练状态机。
- [x] 实现知识类型、受众、产品版本、生效时间和版本链。
- [x] 实现 `draft/pending/rejected/published/offline` 状态机。
- [x] 实现训练完成校验、禁止自审、驳回原因和并发状态条件更新。
- [x] 实现发布/下架时治理状态、审计和 collection `forbid` 的事务联动。
- [x] 实现同版本组旧知识下架和新版本生效。
- [x] 链接复用现有导入、训练、检索测试 UI，新增治理列表、草稿编辑和审核队列。

### 3.3 权限

- [x] 实现 `customerServiceAdmin/knowledgeEditor/knowledgeReviewer` 最小岗位绑定和变更审计。
- [x] 实现编辑/审核岗位互斥和团队 owner 管理员兜底。
- [x] 编辑动作叠加 dataset write，审核动作叠加 dataset manage。
- [x] 管理动作叠加团队/app 权限。
- [x] 完成岗位互斥、资源范围、禁止自审和越权负向测试。

## 4. 阶段四：客服问答和业务 API

### 4.1 项目与 Key binding

- [x] 实现 CustomerServiceProject，引用现有 FastGPT App。
- [x] 实现 CustomerServiceKeyBinding，引用现有 OpenAPI Key。
- [x] 复用现有 Key 创建、应用绑定、过期、额度、统计和管理页面。
- [x] 实现最高受众、启停、来源限制和人工联系方式配置。
- [x] 实现推荐问题和会话保留策略配置。
- [x] 实现项目与 Key 双维度频率限制，并继续复用现有额度和使用统计。
- [x] 已绑定客服项目的 Key 调用通用 v1/v2 completions 时必须拒绝。

### 4.2 最小检索扩展

- [x] 实现已发布 collection 白名单查询。
- [x] 在统一搜索入口增加仅内部使用的 `collectionIdWhitelist`。
- [x] 将白名单与 dataset、型号绑定、metadata filter 和现有 `forbid` 结果求交集。
- [x] embedding/full-text 共用同一交集并继续使用现有 Rerank。
- [x] 空白名单、查询失败和团队不匹配时 fail-closed。
- [x] 验证未传白名单的普通 FastGPT 搜索行为完全不变。

### 4.3 客服 API 适配

- [x] 建立 customer-service OpenAPI schema 和统一导出。
- [x] 实现 products、chat、feedback、health 薄适配接口。
- [x] 所有新增 API 使用 `parseApiInput`、请求/响应 Zod schema 和 OpenAPI 路由声明。
- [x] 实现业务页面预传、请求和会话上下文的产品/版本解析及上下文冲突拒绝。
- [x] 实现型号歧义和版本敏感问题的结构化追问。
- [x] 实现危险、争议、投诉、人工请求和连续低置信度规则。
- [x] 复用现有 App/LLM、流式协议、引用、会话、反馈、usage、费用和日志。
- [x] 将 requestId 映射到稳定消息标识，支持重放、冲突检测、失败重试和过期锁回收。
- [x] 在现有 metadata/日志中补充项目、产品、受众、引用和人工状态。
- [x] 增加外部用户 HMAC 标识和资料导入脱敏工具，密钥只在服务端使用。
- [x] 完成受众、型号、版本、白名单、转人工和通用 API 绕过测试。

## 5. 阶段五：当前项目接入

- [x] 基于现有聊天与 Markdown 能力创建客服入口和品牌包装。
- [x] 支持业务页面通过服务端接口传入产品型号和版本。
- [x] 支持用户选择系列、型号、硬件版本和软件版本。
- [x] 复用流式 Markdown、引用、标签页会话恢复、点赞和点踩。
- [x] 增加结构化追问、安全警告、低置信度和人工客服卡片。
- [!] 高频问题或“未解决”自动预填知识草稿作为后续增强项；一期可从反馈和日志人工创建草稿。
- [x] 显示并管理项目推荐问题。
- [!] 复用现有日志并写入客服 metadata；专用组合筛选面板作为后续增强项。
- [x] 站内浏览器通过同源服务端代理调用，不下发 OpenAPI Key。
- [x] 完成多轮、刷新、网络错误处理和响应式移动端实现；真实浏览器兼容验收待 staging 环境。

## 6. 阶段六：资料导入与问答调优

- [x] 提供产品主档、操作说明、FAQ、故障卡和服务政策模板。
- [!] 获取并脱敏三个试点产品的真实资料。
- [x] 提供导入前脱敏脚本，覆盖姓名、手机号、地址、订单号、身份证号和付款信息等敏感内容。
- [!] 使用真实试点资料执行导入、训练、检查分块并修正资料。
- [x] 在模板和实施说明中约定 PDF、扫描件、图片和视频资料的处理规则。
- [x] 建立正常、歧义、越权、版本冲突、危险和无答案场景矩阵。
- [x] 提供可重复校验的 200 条标准问题矩阵和评测运行器。
- [!] 使用真实问答结果调优切片、混合检索权重、Rerank、阈值和提示词。
- [!] 根据真实反馈将高频未解决问题整理为知识草稿并重新审核。
- [!] 运行真实题集后输出正确率、Recall@5、引用准确率、型号混淆、编造率和转人工报告。

## 7. 阶段七：生产上线

- [x] 确认生产域名 `support.pkiln.com`、现网服务器、HTTPS 和本机加密权限备份目录。
- [!] 确认会话、问题、附件和外部用户标识的保留及删除策略。
- [x] 基于现有 Compose 准备生产覆盖配置和环境变量模板，不提交真实密钥。
- [x] 提供持久卷、重启策略、HTTPS/Nginx、日志和容量配置说明。
- [x] 提供 MongoDB、PostgreSQL/PGVector、Redis、MinIO 和 AI Proxy 健康检查脚本。
- [x] 提供 MongoDB、PGVector、AI Proxy 数据库和 MinIO 备份脚本。
- [x] 提供应用回滚、数据库恢复和对象文件恢复操作说明。
- [x] 实现请求保留期限和每日清理任务；真实附件关联清理需按最终保留政策验收。
- [!] 在 staging 使用真实域名、密钥和数据运行升级、备份及回滚演练。
- [x] 备份现网 MongoDB、PGVector、AI Proxy PostgreSQL、MinIO 和 Compose 配置，记录原版 `v4.15.4` 回滚镜像。
- [x] 构建 ARM64 生产镜像，通过隔离候选容器冒烟测试后仅替换 `fastgpt-app`，保留全部依赖容器和数据卷。
- [x] 验证生产域名首页、客服页、客服管理页均为 200，未授权客服 API 为 403，应用与依赖容器均健康且核心数据数量一致。
- [!] 完成三个真实产品、客服项目/Key、200 题业务联合验收和持续上线观察。

## 8. 最终质量门禁

- [x] 所有一期代码 TODO 已完成；外部输入项和两个后续增强项已明确标记。
- [x] 原需求阶段一至阶段七均已有代码、配置、模板或部署交付物。
- [x] 没有重复实现 FastGPT 已有训练、Key、聊天、反馈、日志或费用能力。
- [x] 所有新增 API 具备后端鉴权、Zod/OpenAPI 和负向测试。
- [x] public/dealer/internal、状态、型号和版本隔离局部测试通过。
- [!] 三个真实试点产品的 200 题质量目标等待资料、模型渠道和 staging 环境。
- [x] 局部测试、类型检查、定向 lint、生产构建以及 app/service/global 有效全量测试通过。
- [x] 二次开发修改清单、数据库、API、环境、部署、升级、回滚和备份文档完整。
- [!] 用户、技术、安全和运维最终验收等待 staging/生产环境。

### 门禁执行说明

- 有效全量测试：`app` 188 文件/1452 用例、`service` 309 文件/3701 用例、`global` 84 文件/1832 用例全部通过；另有 22 个既有集成用例按环境配置跳过。
- 根目录 `pnpm test` 的现有脚本固定筛选了 workspace 中不存在的 `@fastgpt/admin`；移除该无效筛选后，同一 Mongo 隔离环境下的三个实际工作区全量测试通过。
- 根目录 `pnpm lint` 受现有 `projects/marketplace` 的 `next lint` 脚本与当前 Next.js CLI 不兼容影响；本次修改涉及的 app 文件已全部通过定向 ESLint。
- 生产构建退出码为 0；Turbopack 仍提示项目既有的 `styled-jsx/style.js` 解析警告，不影响构建产物生成。

## 9. V1.2 FastGPT 原生创建与智能客服标准模板

当前状态：用户决定暂不接 Rerank 后，无 Rerank 可交付项已完成。标准工作流、模板注册、原生创建入口、固定分支兼容、源 App V1.3.1 发布和生产 Key 绑定均已上线。现有源 App 为 `企业产品智能客服（标准工作流）`（App ID：`6a7acee8b28b311d5ef28803`）。

### 9.1 规划

- [x] 确认智能客服直接复用 FastGPT Workflow 创建和工作台。
- [x] 核对 `community-CQ` 的 6 个节点、示例分类和当前检索配置。
- [x] 确认原模板只能作为骨架，不能原样上线。
- [x] 确认制作本地客服标准模板，不新增 AppType、编辑器或工作流节点。
- [x] 明确模板负责对话流程，客服 API 负责服务端安全边界。
- [x] 用户批准本版模板改造方案。

### 9.2 标准模板

- [x] 使用原生节点创建一个普通 Workflow 模板源 App，并绑定现有“产品库”。
- [x] 将问题分类改为产品咨询、使用配置、故障、售后、问候、危险/人工和非业务。
- [x] 将知识搜索改为混合检索和问题优化。
- [!] Rerank 按用户决定延后接入；当前模板显式关闭，后续有可用模型后再开启和调优。
- [x] 增加引用结果为空的条件判断和资料不足追问。
- [x] 增加危险/人工固定回复和非业务范围回复。
- [x] 配置禁止猜测、型号版本不混用、引用和安全要求的客服提示词。
- [x] 配置欢迎语、推荐问题、历史轮数、图片输入和回答长度。
- [x] 收紧检索上下文，增加长清单完整条目分页和指定系列边界规则，保留 1200 Token 防截断硬上限。
- [-] 验证正常、无资料、型号不明、危险、人工、投诉和无关问题分支；除型号不明和完整题集外均已完成结构或真实接口冒烟。
- [x] 注册并部署 `commercial-customer-service-standard-v1` 本地模板。

### 9.3 创建入口

- [x] 应用列表增加“创建智能客服”入口。
- [x] 通过 `appType=workflow&scene=customerService` 进入现有创建页。
- [x] 客服场景显示“创建智能客服”并推荐本地标准模板。
- [x] 继续复用模板列表、模板详情和 `postCreateApp`，不新增创建 API。
- [x] 创建成功后继续进入原生 `/app/detail` 工作台。

### 9.4 联调与兼容

- [x] 验证原生工作台可以选择知识库、模型，完成调试、保存、发布、日志和版本管理。
- [x] 通过客服 API 调用新 App，验证产品/版本/受众白名单仍生效。
- [x] 验证无引用、低置信度和转人工规则；连续低置信度已有自动化覆盖。
- [x] 通过场景参数隔离、类型检查和生产构建验证普通 Agent、简易应用和其他 Workflow 创建逻辑不变。
- [x] 现有客服页面和 API 保持兼容。
- [x] 完成相关局部测试、类型检查、定向 lint 和生产构建。
- [x] 已在生产完成小流量冒烟并绑定标准工作流，保留旧客服项目及旧镜像作为回滚路径。

### 9.5 V1.3 固定分支与上线门禁

- [x] 客服 API 从完整工作流结果识别问候、非业务、资料不足和人工/安全终止节点。
- [x] 问候和非业务固定回复不再因零引用被覆盖；知识回答仍执行引用和置信度门禁。
- [x] 人工/安全分支返回 `human_required`，资料不足分支继续累计低置信度次数。
- [x] 同步标准源 App 编辑态和发布态的无 Rerank、1600 检索上下文、1200 回答上限、分页及系列边界提示词。
- [x] 新版代码部署并通过客服 API 冒烟后，才将生产 Key 绑定至标准工作流项目。

### 9.6 生产验收记录（2026-08-12）

- [x] 发布工作流 `客服工作流 V1.3.1（无 Rerank，收紧系列边界）`。
- [x] 无 Rerank 实测最高向量分为 `0.424`，项目低置信度阈值由 `0.45` 调整为 `0.35`；后续接入 Rerank 后重新标定。
- [x] 在已发布品牌手册集合内补充一条结构化价格问答，修复 PDF/OCR 导致的配置与价格错位。
- [x] 价格问题返回六组正确对应价格、`answered` 和 3 条真实引用；问候、非业务和转人工分支均通过。
- [x] 部署镜像 `fastgpt-customer-service:20260812-1535`；应用、MongoDB、域名客服页和创建入口健康。
- [!] 当前 Grok 模型知识回答实测 49～94 秒，后续更换低延迟模型或优化模型渠道；这不是本轮 Rerank 上线门禁。

## 10. V1.4 智能客服产品化控制台

- [x] 新增总览、客服管理、知识中心、产品管理、审核中心和系统设置六个业务模块。
- [x] 新增三步客服创建向导，自动使用标准工作流和所选产品知识范围。
- [x] 服务端在同一事务内创建 Workflow App、客服项目、专用 Key 和绑定，失败不留半成品。
- [x] 产品、知识、审核和客服列表默认只显示名称、状态及业务关系，不展示内部 ID。
- [x] 提供按名称选择知识库/资料、创建产品与知识、审核发布、启停及测试入口。
- [x] 桌面和移动端主导航默认进入新控制台；旧管理页和 FastGPT 原生工作台保留为高级入口。
- [x] 通过 54 项相关测试、类型检查、定向 ESLint、生产构建和线上登录态/业务接口冒烟。
- [x] 部署生产镜像 `fastgpt-customer-service:20260813-1231`；App/Mongo healthy，旧镜像可即时回滚。

## 11. V1.5 双前端产品闭环整改（代码已完成，部署收口中）

### 11.1 文档与边界

- [x] 逐项核对原需求第十章客户端和管理端功能。
- [x] 明确交付客户咨询端与管理运营端两套前端，同一应用部署但页面、导航和权限隔离。
- [x] 明确 FastGPT 原生 App、Dataset、日志和 Key 页面只作为管理端高级工具。
- [x] 明确公开访问最高为 public，dealer/internal 必须依赖服务端可信授权。
- [x] 更新需求设计、功能开发、项目规划、TODO 和 Trellis 任务文档。

### 11.2 客户咨询端

- [x] 新增 `/customer-service/chat/[projectCode]` 固定项目正式入口和仅返回单项目的 bootstrap。
- [x] 公共页面禁止项目枚举、浏览器 Key 和 audience 提权；补公开/受控访问负向测试。
- [x] 整理客户侧独立布局、品牌头部、欢迎语、推荐问题、系列/型号/版本级联选择。
- [x] 复用安全 SSE/Markdown/会话，并补发送状态、停止生成、35 秒无数据超时、120 秒总时限、原位幂等重试和业务错误页。
- [x] 展示可展开引用、安全警告和人工卡片；人工联系方式在页头和会话区常驻。
- [x] 支持点赞、点踩和独立 `unresolved`，保证同一回答反馈幂等。
- [x] 支持刷新恢复、新会话和切换产品时的上下文隔离提醒。
- [x] 完成品牌、响应式布局和基础无障碍实现；真实 viewport/axe 验证列入部署前手工门禁。

### 11.3 管理端知识和客服就绪

- [x] 实现选择产品、资料类型、受众、知识库和文件的导入向导。
- [x] 复用原生上传、Collection 创建与训练状态；展示处理中、成功、失败及重试。
- [x] 上传完成自动创建治理草稿；识别上传成功但未登记的资料并允许恢复。
- [x] 将资料提交、审核、发布和下架串联在知识中心和审核中心。
- [x] 产品 Dataset 绑定变化后自动同步相关 Workflow dataset 节点。
- [x] 同步失败保留旧配置并展示原因、时间和重试入口。
- [x] 客服列表增加 App、工作流、模型、已发布知识、知识范围和 Key 的完整就绪检查。
- [x] 客服管理提供复制正式地址和打开客户预览。

### 11.4 管理端对话运营

- [x] 新增对话运营模块和共享 OpenAPI/Zod 合约。
- [x] 联合读取客服请求、原生聊天、引用、反馈和 usage；不复制原生 chat/chatItem，仅保留脱敏运营投影。
- [x] 支持按项目、产品、型号、时间、反馈、无答案、未解决和转人工筛选。
- [x] 展示问题、回答状态、引用、Token/可用用量、耗时和反馈详情。
- [x] 支持将高频/未解决问题预填为知识草稿，保留来源会话和产品上下文。
- [x] 验证运营转草稿后仍必须经过编辑、提交和审核发布。

### 11.5 团队、工作台和收口

- [x] 管理端按团队成员姓名分配、启停客服管理员、知识编辑和知识审核岗位。
- [x] 验证岗位互斥、提交人禁止自审、服务端权限和审计记录。
- [x] 工作台增加无答案、未解决、转人工、同步异常和系统健康待办。
- [x] 日常模块隐藏内部 ID 和不可用商业版入口，保留清晰的高级工作台边界。
- [x] 更新中英繁体文案、基础无障碍和二次开发修改清单；日文资源仍为后续国际化输入。

### 11.6 测试、构建与部署

- [x] 每个增量运行对应 global/service/app 局部测试和 typecheck，不在中途运行全量测试。
- [x] 使用服务层和 API 样例覆盖“上传发布→客户端命中→未解决→运营转草稿→重新审核”主要跨端状态。
- [x] 最后运行格式、定向 lint、有效全量测试、生产构建和部署前检查。
- [ ] 备份现网，构建新镜像，在隔离候选容器冒烟后灰度替换 `support.pkiln.com`。
## 12. V1.7 原生分散嵌入前端全功能闭环

- [x] 在知识库大厅 (`/dataset/list`) 顶部新增产品与型号管理 (`/dataset/product`) 及对话运营中心 (`/dataset/operations`) 快捷入口。
- [x] 重构落地产品管理工作台 (`ProductStudio`)：产品大类/系列/型号/版本层级拓扑树、型号详情看板、别名管理、启停用切换与知识库绑定弹窗。
- [x] 重构落地对话运营工作台 (`OperationsStudio`)：效能与消耗趋势 (`MetricsTrendCards`)、转人工归因分析 (`HandoffReasonChart`)、Badcase 语义聚类 (`BadcaseClusteringList`)、会话多维筛选与一键转知识草稿 (`OneClickToDraftModal`)。
- [x] 打通知识库大厅、采编台 (`/dataset/editor`)、审核台 (`/dataset/reviewer`)、产品管理 (`/dataset/product`)、对话运营 (`/dataset/operations`) 之间的跨页互通导航。
- [x] 完成 `@fastgpt/app` 全量 TypeScript 类型检查 (0 错误) 与智能客服单元测试全部通过。

## 13. V1.8 知识采编与 FastGPT 原生知识库一体化深度融合

- [x] 在原生知识库详情页 (`/dataset/detail`)「新建与导入」菜单增加 4 大企业产品结构化模板（产品主档、操作手册、故障排查卡、FAQ批量编辑器）。
- [x] 在原生集合列表 (`CollectionCard`) 增加「客服治理状态」列（展示草稿/待审/已发布/已驳回 Tag 与受众 Tag）。
- [x] 在原生集合行操作菜单增加「提交客服审核」与「登记为客服知识」动作，完成从上传到提审的原生闭环。
- [x] 完成生产镜像构建 `fastgpt-customer-service:20260819-v2.0` 并重启部署验证通过。

## 14. V1.9 导航角色权限精准隔离与知识采编台入口精简

- [x] 彻底移除顶部冗余的「知识采编台」按钮（已全面融入知识库详情页「创建和导入」菜单与集合列表）。
- [x] 实现顶部导航按钮的角色能力精准隔离（基于 `GET /api/customer-service/admin/me`）：
  - **【知识审核台】**：仅对具备 `reviewKnowledge` 权限的「知识审核员」及管理员/Owner 显示。
  - **【产品管理】**：仅对具备 `manageProjects` 权限的「客服管理员」及 Owner 显示。
  - **【对话运营】**：仅对具备 `viewOperations` 权限的「客服管理员」及 Owner 显示。
  - **普通成员**：仅展示纯净的原生知识库页面，不暴露任何无权访问的业务按钮。
- [x] 重定向旧路由 `/dataset/editor` 与 `/customer-service/editor` 直接回流知识库大厅。
- [x] 完成全套 TypeScript 类型检查 (0 错误) 与自动化测试全部通过。
- [x] 构建生产镜像 `fastgpt-customer-service:20260819-v2.1` 并热更新上线。

## 15. V2.0 修复知识召回与索引检索致命阻断 Bug

- [x] 彻底定位并解决召回拦截问题：
  - 修复 [`packages/service/core/customerService/knowledge/guard.ts`](file:///root/FastGPT-source/packages/service/core/customerService/knowledge/guard.ts) 中 `getCustomerServiceGovernedCollectionIds` 无差别排除已发布知识的严重缺陷，仅过滤未发布草稿/待审/下架记录。
  - 修复 [`packages/service/core/dataset/search/defaultRecall/multiQueryRecall.ts`](file:///root/FastGPT-source/packages/service/core/dataset/search/defaultRecall/multiQueryRecall.ts)，确保已发布知识或普通启用集合均能被原生搜索测试、应用对话、工作流数据集搜索正常召回。
  - 修复审核试问沙盒中显式 `collectionIdWhitelist` 被全局 forbid 误伤的问题，支持草稿即时试问。
- [x] 增强 4 大结构化模板（产品主档、操作手册、故障排查卡、FAQ批量）的完整 Markdown 同步渲染，生成高质量切片。
- [x] 构建生产镜像 `fastgpt-customer-service:20260819-v2.2` 并平滑升级上线。

## 16. V2.1 审核员与采编员极简门户改造与直达审核台

- [x] **审核员 (`knowledgeReviewer`) 极简导航与直达审核**：
  - 彻底隐藏「对话/门户」(Chat) 与「工作台」(Studio) 入口。
  - 左侧主导航提供：**【知识审核】** (`/dataset/reviewer`)、**【知识库】** (`/dataset/list`)、**【账号】** (`/account/info`)。
  - 登录后与首页访问 (`/`) 默认直达 **【知识审核】** (`/dataset/reviewer`)，无需通过知识库层层点击。
  - URL 防越权拦截：直接在浏览器输入 `/dashboard` 或 `/chat` 时自动平滑重定向至 `/dataset/reviewer`。
- [x] **采编员 (`knowledgeEditor`) 极简知识采编导航**：
  - 彻底隐藏「对话/门户」(Chat) 与「工作台」(Studio) 入口。
  - 左侧主导航提供：**【知识库】** (`/dataset/list`)、**【账号】** (`/account/info`)。
  - 登录后与首页访问 (`/`) 默认直达 **【知识库】** (`/dataset/list`)。
  - URL 防越权拦截：直接输入 `/dashboard` 或 `/chat` 时自动平滑重定向至 `/dataset/list`。
- [x] 构建生产镜像 `fastgpt-customer-service:20260819-v2.3` 并热更新上线。

## 17. V2.2 审核员极致专注审核流（全面移除知识库大厅入口）

- [x] **知识审核员 (`knowledgeReviewer`) 纯粹审核视图**：
  - 彻底移除左侧主导航栏的「知识库」入口，审核员左侧导航仅保留：**【知识审核】** (`/dataset/reviewer`) 与 **【账号】** (`/account/info`)。
  - 顶部导航栏移除「返回知识库」跳转链接，防止越权或分散审核注意力。
  - 路由全面封闭拦截：审核员访问 `/dataset/list`、`/dataset/detail`、`/dashboard`、`/chat` 时，全自动平滑重定向至 `/dataset/reviewer`。
- [x] 构建生产镜像 `fastgpt-customer-service:20260819-v2.4` 并热更新部署上线。

## 18. V2.3 修复管理员被锁死在审核台与全角色自由导航

- [x] **彻底修复管理员与多角色身份识别与权限隔离**：
  - 修复 `api/customer-service/admin/me.ts`：将 FastGPT 管理员权限 (`hasManagePer`) 正确纳入 `isTeamOwner`/`isAdmin` 计算，杜绝团队管理员被误识别为普通成员或仅审核员。
  - 修复 `navbar.tsx` 与 `navbarPhone.tsx`：管理员账号（Owner/Admin/Root/拥有 `manageProjects` 能力者）永远展示全量主导航（门户/工作台/知识库/账号），且知识库直接指向 `/dataset/list`。
  - 修复 `Layout/index.tsx`：移除对 `/dataset/list` 和 `/dataset/detail` 的全局霸道拦截重定向，仅对纯审核员拦截未开放的 `/dashboard` 和 `/chat`，彻底解除锁死死循环。
  - 恢复 `dataset/reviewer` 顶部的「返回知识库」按钮，支持管理员和使用者在知识库与审核台之间自由切换。
- [x] 构建生产镜像 `fastgpt-customer-service:20260819-v2.5` 并平滑升级上线。







