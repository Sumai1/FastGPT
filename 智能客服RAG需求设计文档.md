# 企业产品智能客服 RAG 知识库系统需求设计文档

文档版本：V1.5（双前端产品闭环整改版）  
状态：V1.5 代码整改已完成，正在进行最终质量门禁、构建与灰度部署  
候选基线：FastGPT `f88b51b5c`

## 1. 目标与边界

系统为拍照机、售货机等无人设备提供有依据的产品问答和售后指导。回答必须受产品型号、软硬件版本、访问受众和知识发布状态约束；无可靠依据、存在安全风险或满足业务转人工条件时不得继续猜测。

一期覆盖原需求阶段一至阶段七。FastGPT 已有 dataset、collection、导入训练、RAG、App Chat、会话、反馈、日志、OpenAPI Key、权限和部署能力全部复用。

一期不包含自动退款、订单修改、远程控制、完整工单、多租户商业计费、自研模型/向量库、OCR、图像故障识别和大规模 RAG 内核重写。

## 2. 角色与权限

FastGPT 资源权限负责数据范围；最小客服岗位绑定负责业务动作。原因是现有 manage 权限同时包含 write，不能严格表达编辑与审核互斥。

| 业务岗位 | FastGPT 权限基础 | 允许操作 | 额外业务限制 |
|---|---|---|---|
| 客服管理员 | 团队 owner/管理权限 | 项目、Key 绑定、规则、日志、紧急发布/下架 | 紧急操作必须填写原因并审计 |
| 知识编辑 | 目标 dataset write 权限 | 导入、编辑治理信息、提交审核、检索测试 | 不能审核和发布自己提交的版本 |
| 知识审核 | 目标 dataset manage 权限 | 审核、驳回、发布、下架和冲突处理 | 不能审核自己提交的版本 |
| 普通团队成员 | 现有 read 或无权限 | 按原 FastGPT 权限使用资源 | 默认没有知识治理动作 |

管理 API 同时校验团队成员身份、目标 dataset/app 权限、知识状态和禁止自审规则。前端按钮可见性不能代替服务端校验。

岗位记录按 `teamId + tmbId` 保存 `customerServiceAdmin`、`knowledgeEditor` 或 `knowledgeReviewer`。编辑与审核岗位互斥；团队 owner 默认视为客服管理员。岗位变更记录操作者、变更前后值、原因和时间，但不复制 FastGPT 团队成员或资源协作者数据。

## 3. 外部受众

现有 FastGPT OpenAPI Key 通过客服 Key binding 绑定最高访问等级：

```text
public   → 公开产品、基础操作、客户可执行排查、公开政策
dealer   → public + 补货、耗材、巡检、运营和基础设置
internal → public + dealer + 错误码、维修和内部规范
```

- 请求可降低等级，不可提高等级。
- 客服绑定 Key 只能进入客服业务接口。
- 外部请求不得提供 dataset ID、collection ID 或检索表达式。
- Key 的明文、到期时间、额度和使用量由现有 FastGPT OpenAPI Key 维护。

## 4. 业务对象

### 4.1 产品

- 产品大类：`code`、`name`、`description`、`status`、`sort`。
- 产品系列：`categoryId`、`code`、`name`、`aliases`、`description`、`status`、`sort`。
- 产品型号：`seriesId`、`modelCode`、`name`、`aliases`、`intro`、`status`、`discontinuedAt`、`datasetIds`。
- 产品版本：`modelId`、`type`（hardware/software）、`versionCode`、`name`、`description`、`status`、`effectiveFrom`、`effectiveTo`。

型号编码全局唯一。停用、停产和历史版本保留已有知识与会话引用。

### 4.2 知识治理记录

每份可治理文档、FAQ 或故障卡引用一个现有 FastGPT collection；正文、chunk、向量和训练任务不复制到客服业务集合。

治理字段：

- `title`、`knowledgeType`、`audienceLevel`。
- `datasetId`、`collectionId`、`sourceName`。
- `productModelIds`、`hardwareVersionIds`、`softwareVersionIds`。
- `allHardwareVersions`、`allSoftwareVersions`。
- `status`：`draft`、`pending`、`rejected`、`published`、`offline`。
- `version`、`versionGroupId`、`previousVersionId`。
- `effectiveFrom`、`effectiveTo`。
- `createdBy`、`updatedBy`、`submitterTmbId`、`reviewerTmbId`。
- `submitTime`、`reviewTime`、`publishTime`、`offlineTime`、`auditReason`。

知识类型包括产品主档、产品参数、操作说明、FAQ、故障排查卡、错误码、耗材与维护、安全规范、售后政策、客服话术和内部维修资料。

训练状态直接读取 FastGPT collection 的 `running/error/ready`，不在治理记录中重复维护。

### 4.3 客服项目和 Key binding

客服项目字段：`name`、`code`、`status`、`appId`、`modelIds`、`defaultAudience`、`rateLimitOverride`、`recommendedQuestions`、`humanContact`、`workingHours`、`ruleConfig`、`chatRetentionPolicy`。

Key binding 字段：`openApiKeyId`、`projectId`、`maxAudience`、`status`、`allowedOrigins`、`createdBy`、`disabledReason`。

Key 创建、显示、过期、额度和使用统计沿用 FastGPT 原有页面及服务；客服管理只维护绑定关系。

### 4.4 会话业务上下文

复用 FastGPT chat/chatItem/node response。优先在允许的 metadata 中记录：

- `projectId`、`requestId`、`externalUserId`。
- 产品大类、系列、型号及软硬件版本 ID。
- `audience`、低置信度次数、转人工标志及原因。
- 有效引用数和业务错误阶段。

仅当现有日志筛选无法满足性能要求时增加轻量索引表，不复制会话正文。

## 5. 知识生命周期

```text
draft → pending → published → offline
   ▲       │
   └── rejected
```

1. 客服导入入口复用现有 collection 创建、文件/QA 导入和训练队列，并将新 collection 设置为 `forbid=true`。
2. 通用 FastGPT 导入入口保持不变；没有治理记录的 collection 不在客服白名单中。
3. 训练状态为 `ready`、内容非空且治理字段完整时才能提交审核。
4. 审核拒绝必须填写原因；审核人不能等于该版本提交人。
5. 发布更新治理状态、审计记录并切换 collection `forbid=false`。
6. 下架切换为 `offline + forbid=true`。
7. 发布冲突新版本时，旧版本下架和新版本生效在同一事务完成。
8. 修改已发布知识创建新治理版本；旧版本在新版本发布前继续服务。

首期导入格式沿用 FastGPT 现有能力：Markdown、TXT、DOCX、可提取文本 PDF、XLSX/CSV、手工内容和 QA。旧版 Word/Excel 先转换为支持格式；扫描 PDF 和图片说明书由资料人员先完成 OCR 与人工校对再导入，不在一期开发 OCR。

## 6. 客服问答规则

### 6.1 产品与版本解析

1. 优先使用业务页面服务端传入的型号和版本。
2. 其次使用请求字段和当前会话上下文。
3. 只有系列、别名歧义或多个型号匹配时返回候选并追问。
4. 未提供版本时只允许通用版本知识。
5. 问题涉及参数、错误码、维修或固件差异时追问相关版本。
6. 禁止把不同型号或版本的资料合并成确定答案。

### 6.2 collection 白名单

白名单条件必须同时满足：

- dataset 在客服项目和当前产品允许范围内。
- 治理状态为 `published`，当前时间位于有效期内。
- 受众等级不超过 Key 最高等级和本次请求等级。
- 型号匹配，或知识明确标记为多型号通用。
- 硬件、软件版本匹配，或相应知识标记适用于全部版本。
- collection 未被 FastGPT 设置为 `forbid=true`。

任一条件查询异常或结果为空时返回空召回，不回退全库。

### 6.3 RAG 处理

客服适配层计算 collection ID 白名单后调用 FastGPT 现有统一搜索、混合召回和 Rerank。内部白名单与现有 dataset 范围、metadata filter 和 `forbid` 结果求交集，同时作用于 embedding 和 full-text。

白名单参数不暴露到外部 API，不允许由模型或普通工作流变量产生。普通 FastGPT App 和 Agent 保持现状；客服一期只保证客服专用执行路径的安全边界。

### 6.4 回答要求

- 产品事实必须有有效引用支持。
- 操作问题使用编号步骤。
- 危险操作显示警告并停止继续排查。
- 无有效资料时明确说明无法确认，不编造答案。
- 引用应支持回答中的关键结论。
- 默认回答不超过 600 Token，可按项目配置调整。

### 6.5 转人工

服务端直接触发：

- 用户明确要求人工。
- 支付、退款、保修责任、投诉或明显激烈情绪。
- 拆机、高压、制冷剂、内部密码等危险操作。
- 焦味、冒烟、漏电、严重异响等危险现象。
- 默认连续两次没有有效引用或低于置信度阈值。

型号或必要版本缺失只追问，不直接转人工。模型不能撤销服务端已确定的转人工结果。

## 7. 外部 API

一期接口：

```text
GET  /api/customer-service/v1/products
POST /api/customer-service/v1/chat
POST /api/customer-service/v1/feedback
GET  /api/customer-service/v1/health
```

Chat 请求核心字段：

```json
{
  "sessionId": "optional-session-id",
  "message": "设备如何更换打印耗材？",
  "stream": true,
  "externalUserId": "opaque-user-id",
  "productModel": "DT-2026A",
  "hardwareVersion": "V2",
  "softwareVersion": "V3.1",
  "audience": "public"
}
```

响应在 FastGPT 现有聊天和流式结果基础上补充：

- `requestId`、`sessionId`、`messageId`。
- `status`：`answered | clarification_required | human_required`。
- 已解析产品/版本、引用、安全警告和人工联系方式。
- Token、费用和耗时继续采用现有 usage/log 数据。

如外部业务确需会话查询或删除，再复用现有 chat service 增加适配路由，不建立新会话存储。

调用方可以提供 `requestId` 作为幂等键。相同项目、Key 和 requestId 的重试必须复用或返回已存在的消息结果，不得重复执行模型调用和计费；实现时复用 FastGPT 现有 responseChatItemId/dataId 唯一性机制。

会话、原始问题、附件和外部用户标识的保留期限在生产上线前由业务和安全人员确认。删除操作复用现有 chat 删除能力；需要自动到期清理时只增加调用现有删除 service 的计划任务，不复制会话数据。

API 安全要求：

- 执行 Key binding 启停、项目级频率限制、额度和可选来源限制。
- 浏览器不保存 OpenAPI Key；生产接口使用 HTTPS。
- 请求体大小、metadata 字段、上传文件类型和大小沿用并收紧现有校验。
- 日志和错误响应隐藏完整 Key、手机号、身份证、地址、订单号和付款信息。
- 普通客服 Key 不允许调用知识管理接口。

## 8. 前端要求

### 8.1 客服端

- 复用 FastGPT 现有聊天、流式显示、Markdown、引用、反馈和历史会话能力。
- 增加品牌、欢迎语、推荐问题、产品/版本选择和业务页面自动带入。
- 增加安全警告、低置信度和人工客服卡片。
- 站内浏览器通过服务端代理调用，不暴露 OpenAPI Key。

### 8.2 管理端

- 新增产品树、版本、dataset 绑定、知识治理和审核队列。
- 新增客服项目、Key 受众绑定、规则和人工联系方式配置。
- 新增推荐问题和会话保留策略配置。
- 直接复用 dataset/collection 导入训练、检索测试、App 配置、Key 管理、日志、反馈和标注页面。
- 日志通过薄查询适配支持按项目、按产品、按型号、引用知识、Token/费用、反馈、无答案和转人工筛选。
- 高频未解决问题可以带入现有知识编辑流程形成“待整理知识（draft）”。

## 9. 验收标准

- 阶段一至阶段七交付物和门禁全部完成。
- 三类受众权限负向测试全部通过。
- 草稿、待审核、驳回和下架知识在两条召回链中不可见。
- 相邻型号、硬件版本和软件版本不串知识。
- 提交人禁止自审，管理员紧急操作完整审计。
- 三个试点产品完成资料导入和审核。
- 不少于 200 个标准问题完成回归。
- 正确率不低于 90%，Recall@5 不低于 90%，引用准确率不低于 95%，明显编造率不高于 2%。
- 危险问题转人工命中率为 100%。
- 最终局部/全量测试、类型检查、lint、构建、部署和恢复演练通过。
- 相同 requestId 重试不产生重复消息或重复费用，会话删除和保留策略验证通过。

## 10. 外部输入

- 模型渠道和测试密钥。
- 三个试点产品的真实型号、版本和资料。
- 操作图片、教程视频链接及其经过确认的文字步骤。
- 脱敏问题集或历史售后问题。
- 人工客服联系方式和工作时间。
- 会话、问题、附件和外部用户标识的保留及删除策略。
- 生产域名、服务器、HTTPS 和备份目标。
- 编辑、审核和管理员测试账号。

外部输入未到位不阻塞领域模型和接口开发，但阻塞真实知识质量、性能和生产恢复验收。

## 11. V1.5 双前端产品闭环整改需求

### 11.1 前端产品边界

原需求第十章明确要求交付两套相互隔离的业务前端，但不要求拆成两个代码仓库或两套部署：

| 前端 | 使用者 | 入口 | 登录与权限 | 核心目标 |
|---|---|---|---|---|
| 客户咨询端 | 普通客户、经销商、内部售后 | `/customer-service/chat/{projectCode}` | 不暴露 FastGPT 登录页或 OpenAPI Key；由服务端按项目和访问受众鉴权 | 找产品、提问题、看依据、反馈、转人工 |
| 管理运营端 | 客服管理员、知识编辑、知识审核 | `/customer-service/console` | 使用 FastGPT 团队登录态并叠加客服业务岗位 | 配客服、管产品、管知识、审核、看对话、处理问题 |

FastGPT 原生 Workflow、Dataset、App 日志和 Key 页面是管理运营端可进入的“高级工作台”，不是面向客户的第三套前端，也不能代替管理运营端的日常业务页面。

现有 `/customer-service` 仅作为登录态测试页或兼容重定向，不再作为正式客户入口；正式客户入口由项目编码锁定客服，不允许客户选择团队中的其他客服项目。

客户咨询端只有一套交互，不按 public、dealer、internal 再复制三套页面。公开项目地址的最高受众固定为 public；经销商和内部售后必须通过业务系统登录态、服务端签名的短期访问凭证或受控嵌入入口获得对应受众。`projectCode` 不是密钥，URL 参数和浏览器表单不能提升受众。

### 11.2 客户咨询端功能

客户咨询端必须完整覆盖原需求 10.1，并满足以下交付口径：

1. 每个启用的智能客服都有固定访问地址，打开后直接进入该客服，不显示项目选择器、App ID、Dataset、Key 或工作流等内部概念。
2. 首屏展示企业品牌、客服名称、欢迎语、服务时间、推荐问题和“联系人工客服”常驻入口。
3. 支持产品系列、产品型号选择；软硬件版本在该型号存在版本资料时显示。业务系统可通过受控参数预带产品上下文，但服务端仍重新校验。
4. 支持发送文字、回车发送、发送中状态、流式回答、Markdown、代码/表格展示和主动停止生成。
5. 每条知识回答展示可展开的引用资料，至少包含资料标题和引用片段；无可靠引用时显示资料不足或转人工，不伪装成确定答案。
6. 危险、争议、投诉、保修、连续低置信度和用户主动要求人工时，展示明显的安全警告或人工服务卡片。
7. 每条回答支持点赞、点踩和“问题未解决”；同一回答重复操作应幂等。“问题未解决”进入管理端待处理列表。
8. “联系人工客服”在首屏、会话区和人工分支均可访问，并展示项目配置的电话、链接、服务时间等信息。
9. 同一项目支持会话上下文、刷新恢复、新建会话和会话隔离；切换产品时给出是否新建会话的提示，避免上下文串产品。
10. 请求失败、超时、服务停用和无资料时显示可理解的业务提示，并提供重试或联系人工，不暴露服务器错误和密钥信息。
11. 桌面端和移动端均可使用；常用按钮、输入框和人工入口在移动端不被遮挡。

### 11.3 管理运营端功能

管理运营端必须完整覆盖原需求 10.2，并形成以下七个业务模块：

| 模块 | 必须提供的能力 |
|---|---|
| 工作台 | 客服、知识、待审核、无答案、未解决、转人工、同步异常和健康状态总览；展示可执行待办 |
| 客服管理 | 创建、编辑、启停、复制正式地址、打开客户预览；检查 App、工作流、模型、产品知识范围和专用 Key 是否就绪 |
| 知识中心 | 选择产品和资料类型后上传文件或录入问答，查看解析/训练状态，自动生成治理草稿，提交审核、发布、下架和失败重试 |
| 产品管理 | 管理大类、系列、型号、版本和 dataset 绑定；绑定变化后自动同步受影响客服，并展示同步结果 |
| 审核中心 | 查看待审内容、适用产品、受众、版本、引用资料和提交人；禁止自审，支持通过发布、驳回和审计记录 |
| 对话运营 | 查看全部对话；按项目、产品、型号和时间筛选；查看引用知识、Token/用量、耗时、反馈、无答案和转人工记录；将高频问题一键转为待整理知识 |
| 团队与设置 | 按成员姓名分配客服管理员、知识编辑和知识审核；启停岗位；维护人工联系方式；查看系统健康、错误提示和高级工作台入口 |

日常操作不得要求用户复制数据库 ID、App ID、Dataset ID 或 OpenAPI Key。列表、筛选和表单以客服名称、产品名称、资料名称和成员姓名展示；内部 ID 仅作为服务端关联值。

### 11.4 两套前端之间的数据闭环

```text
管理端上传资料并发布
  → FastGPT 完成解析、分块和训练
  → 治理记录发布并进入客服白名单
  → 相关客服工作流知识范围自动同步
  → 客户端固定地址可检索新知识
  → 客户提问、引用、反馈、未解决和转人工被记录
  → 管理端对话运营筛选和处理
  → 高频问题转为知识草稿，再次审核发布
```

只有以上链路可以从管理端完成、在客户端生效、再回到管理端处理，才视为产品闭环完成。跳转到 FastGPT 原生页面后依靠人工复制 ID 或手工拼接资源，不计入闭环验收。

### 11.5 V1.5 整改状态与交付口径

V1.5 的两套前端和三条业务闭环已经在代码中实现，最终交付还需通过全量门禁、生产构建、
灰度和线上冒烟。当前状态如下：

- 客户端使用固定项目地址，只返回单项目公开配置；浏览器不接触 OpenAPI Key，也不能枚举项目或提升受众。
- 管理端已串联资料上传、训练状态、治理草稿、审核发布、同步重试、对话运营和岗位维护；原生页面保留为高级工作台。
- 客户页的“流式”采用安全 SSE：先发送处理状态和心跳，完成引用、置信度及安全判定后再分块发送答案，
  不向客户透传未经门禁的模型 token。客户端有 35 秒无数据超时和 120 秒总时限，停止/超时会提交服务端停止请求。
- `customer_service_requests` 仅保存问题、状态及少量脱敏运营投影，不复制原生 `chat`/`chatItem` 集合；
  原始会话、引用、反馈、usage 仍从 FastGPT 既有链路联合读取。
- 真实三产品资料、200 题质量报告和 Rerank 调优仍是外部输入/后续项，不阻塞本轮前端闭环代码交付。
